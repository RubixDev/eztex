\boxedinputlisting[style=Rust, caption={Ein Rust Beispielprogramm}]{snippets/example.rs}
\begin{boxedlisting}[style=Rust, title={Ein kürzeres Beispiel von innerhalb des \LaTeX\, Codes}]
let mut vector = vec![1, 2, 3u8];
vector.push(4);
\end{boxedlisting}
Verweis auf Dinge wie \rustcode{u8} oder \rustcode{mut} in Text.
