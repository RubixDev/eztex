\boxedinputlisting[style=Rust, caption={An example Rust program}]{snippets/example.rs}
\begin{boxedlisting}[style=Rust, title={A shorter example from inside the \LaTeX\, code}]
let mut vector = vec![1, 2, 3u8];
vector.push(4);
\end{boxedlisting}
References to things like \rustcode{u8} or \rustcode{mut} inside text.
